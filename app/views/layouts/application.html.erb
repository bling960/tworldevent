<!DOCTYPE html>
<html>
  <head>
    <title>SK텔레콤 PS&M 직영매장 EVENT</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no">
    <meta name="author" content="SK텔레콤 PS&M">
    <!-- Open Graph data for Facebook -->
    <meta property="og:type" content="article" />
    <meta property="og:url" content="<%= request.original_url %>" />
    <meta property="og:title" content="SK텔레콤 PS&M 직영매장 EVENT" />
    <% if content_for?(:head) %>
      <%= yield(:head) %>
    <% end %>

    <!-- Twitter Card data -->
    <meta name="twitter:card" content="summary"/>
    <meta name="twitter:url" content="<%= request.original_url %>"/>
    <meta name="twitter:title" content="SK텔레콤 PS&M 직영매장 EVENT" />
    

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <div id="detectWidth"></div>
    <%= yield %>

    <script>
      function gtag(){dataLayer.push(arguments)}window.dataLayer=window.dataLayer||[],gtag("js",new Date),gtag("config","UA-187580792-2");
    </script>
  </body>
</html>
